# @codenanny/cli

Command-line entry point for codenanny.

```bash
codenanny wizard          # browser-based setup wizard (default)
codenanny serve           # live server
codenanny ingest          # one-shot ingest only
codenanny export          # static bundle export
codenanny help            # full options
```

See the [root README](../../README.md) for the full overview.
